[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "burmese-stt-service"
version = "1.0.0"
description = "Internal Burmese-English speech-to-text service for banking support."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
